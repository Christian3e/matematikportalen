{
  "name": "matematikportalen",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css app.js vercel.json dist/",
    "check": "node --check app.js"
  }
}
